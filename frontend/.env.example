# MockMate Frontend — Environment Variables
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

VITE_API_BASE_URL=http://localhost:5000

# Authentication guard — set to true when backend auth endpoints are live.
# When false, all protected routes are accessible without login (dev mode only).
VITE_AUTH_ENABLED=false
